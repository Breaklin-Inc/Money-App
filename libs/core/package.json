{
  "name": "core",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index",
  "files": ["dist"],
  "scripts": {
    "build": "pnpm run clean && pnpm run compile",
    "clean": "rimraf ./dist",
    "prepublish": "pnpm run build",
    "compile": "tsc",
    "publish": "pnpm run prepublish && pnpm publish --no-git-checks"
  },
  "publishConfig": {
    "registry": "https://npm.pkg.github.com/"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^5.1.3"
  }
}
